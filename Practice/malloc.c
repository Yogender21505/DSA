#include <stdio.h>
#include <stdlib.h>

struct node{
    int data;
    struct node *next;
};

int main()
{
    struct node* hea
    int n;
    scanf("%d",&n);
    for ()
    print("%d",call even()+);
    free(ptr);
    ptr=NULL;
}